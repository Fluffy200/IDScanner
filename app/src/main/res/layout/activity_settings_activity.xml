<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.example.fluffy.idscanner.SetingsActivity"
    android:onClick="changeScanMode">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Multi-Scan"
        android:id="@+id/mscan"
        android:textSize="20dp"
        android:layout_alignParentTop="true"
        android:layout_toRightOf="@+id/modeToggle"
        android:layout_toEndOf="@+id/modeToggle" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Opens scanner again automatically after each scan is complete"
        android:id="@+id/textView4"
        android:layout_below="@+id/mscan"
        android:layout_alignLeft="@+id/mscan"
        android:layout_alignStart="@+id/mscan" />

    <ToggleButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="New ToggleButton"
        android:id="@+id/modeToggle"
        android:layout_alignTop="@+id/mscan"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:onClick="changeScanMode"
        android:checked="true" />
</RelativeLayout>
